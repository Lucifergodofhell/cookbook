@model IEnumerable<Webproject.Models.Tweet.Post>

@{
    ViewData["Title"] = "Index";
}
<div class="gifclass">
    <img src="~/images/newgif.gif" hight="700" width="700" >
    <h1>You have Tweeted</h1>
</div>
<!--
<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Tweet)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.istweet)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tweet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.istweet)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TweetID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TweetID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TweetID">Delete</a>
            </td>
        </tr>
}

    </tbody>
</table>
      -->
